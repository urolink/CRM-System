# UroLink CRM 인수인계 가이드

담당자가 이 사이트를 직접 고쳐가며 쓰기 위한 안내입니다.
개발 지식은 필요 없고, **Claude Code에 한국어로 말하면 대신 고쳐줍니다.**

---

## 1. 처음 한 번만 준비 (약 20분)

### ① Claude Code 설치
https://claude.com/claude-code 에서 설치합니다. (Windows 데스크톱 앱 권장)

### ② GitHub 계정 권한 받기
사이트 코드는 GitHub의 `urolink/CRM-System` 에 있습니다.
계정 소유자에게 **Collaborator(Write)** 로 초대받으세요.
초대 메일이 오면 수락합니다.

### ③ 코드 내려받기
Claude Code를 열고, 아래를 **그대로 복사해서 붙여넣고** 보내세요.

```
GitHub의 urolink/CRM-System 저장소를 crm 브랜치로 내 문서 폴더에 clone 해줘.
그리고 로컬 서버로 띄워서 화면이 정상인지 확인해줘.
```

Claude Code가 알아서 받아옵니다. GitHub 로그인을 물으면 안내대로 진행하세요.

> **중요** — 브랜치는 반드시 **`crm`** 입니다. `main` 에는 안 쓰는 옛 코드가 있습니다.

---

## 2. 매일 쓰는 작업 흐름

**폴더를 열고 Claude Code에 하고 싶은 말을 그냥 하면 됩니다.**

작업할 때 이 순서로 말하는 게 가장 안전합니다.

| 순서 | 이렇게 말하세요 |
|---|---|
| 1 | `최신 코드 받아줘` |
| 2 | (고치고 싶은 내용을 자유롭게) |
| 3 | `로컬에서 확인해줘` |
| 4 | `문제 없으면 배포해줘` |

### 실제 예시

```
고객사 목록에 '최근 방문일' 열을 추가해줘.
상담일지에서 그 고객사의 가장 최근 날짜를 가져오면 돼.
```

```
견적서 인쇄할 때 회사 전화번호를 02-1234-5678 로 바꿔줘.
```

```
수주관리 칸반에서 3억 넘는 딜은 카드에 금색 테두리를 넣어줘.
```

```
일정 화면에서 완료된 일정도 색이 너무 흐려서 안 보여. 좀 진하게 해줘.
```

바꾼 게 마음에 안 들면 이렇게 말하면 됩니다.

```
방금 바꾼 거 되돌려줘.
```

---

## 3. 꼭 알아둘 것 3가지

### ⚠ 데이터 백업은 아무도 대신 못 해줍니다
데이터는 Supabase 서버에 있고, **무료 플랜은 자동 백업이 없습니다.**
실수로 지우면 복구가 불가능합니다. 실제 데이터가 쌓이기 시작하면 이렇게 말해서 파일로 받아두세요.

```
지금 데이터 전체를 JSON 파일로 내려받게 해줘.
```

(월 1회 정도, 받은 파일은 회사 드라이브에 보관)

### ⚠ 비밀 키는 절대 코드에 넣지 않습니다
Supabase 화면에 `service_role` 또는 `sb_secret_` 로 시작하는 키가 있습니다.
**이 키를 Claude Code에 알려주거나 코드에 넣으면 안 됩니다.** 데이터가 전부 노출됩니다.
코드에 들어가도 되는 건 `sb_publishable_` 로 시작하는 공개용 키뿐입니다.

### ⚠ 화면이 백지로 나오면
코드에 문법 오류가 생긴 경우입니다. 당황하지 말고 이렇게 말하세요.

```
사이트가 백지로 나와. 마지막 배포를 되돌려줘.
```

---

## 4. 계정 관리 (개발 아님, 화면에서 함)

사이트에 관리자로 로그인 → 왼쪽 맨 아래 **사용자 관리** 메뉴
(관리자에게만 보입니다)

| 하고 싶은 것 | 방법 |
|---|---|
| 직원 계정 만들기 | `사용자 추가` → 이메일·임시비번(자동생성)·이름·부서·직급·역할 |
| 비밀번호 재설정 | 해당 직원 줄의 `비번 설정` → 새 비번 → 전달 |
| 퇴사자 처리 | `차단` (기록은 남고 접속만 막힘) 또는 `삭제` (계정 완전 삭제) |
| 관리자 지정 | 역할을 `관리자` 로 변경 |
| 내 비밀번호 변경 | 왼쪽 아래 계정 카드의 `비밀번호` |

> 등록한 이메일만 가입됩니다. 외부인은 계정을 만들 수 없습니다.

---

## 5. 자주 나오는 요청 모음

그대로 복사해서 쓰셔도 됩니다.

**데이터 정리**
```
샘플로 넣어둔 가상 데이터를 전부 지워줘. 실제 데이터만 남기고 싶어.
```

**화면 추가**
```
'월별 방문 실적' 화면을 새로 만들어줘.
담당자별로 이번 달 방문 건수를 표로 보여주면 돼.
```

**입력 항목 추가**
```
고객사에 '거래 시작일' 항목을 추가해줘.
```

**보고서**
```
이번 달 수주 실적을 A4 한 장으로 인쇄할 수 있게 만들어줘.
```

**문구·색 변경**
```
'수주관리'라는 메뉴 이름을 '영업 파이프라인'으로 바꿔줘.
```

---

## 6. 막혔을 때

Claude Code에 상황을 그대로 말하면 됩니다. 화면 캡처를 붙여도 됩니다.

```
로그인이 안 돼. '이메일 또는 비밀번호가 올바르지 않습니다'가 계속 나와.
```

```
매출 숫자가 이상해. 종합 화면과 매출 화면 금액이 다르게 나와.
```

기술적인 배경은 같은 폴더의 `CLAUDE.md` 에 정리돼 있고,
Claude Code가 그 파일을 자동으로 읽습니다. 담당자가 읽을 필요는 없습니다.
